version: '3'
services:

  cloud-game:
    build: .
    image: "cloud-game"
    privileged: true
    command: bash -c "./coordinator --v=5 & ./worker --coordinatorhost localhost:8000"
    ports:
      - "8000:8000"
      - "9000:9000"
    volumes:
      - ./assets:/assets
